# Public assets

Drop these here when ready:

- `resume.pdf` — picked up automatically by the Resume section's embedded viewer.
- `projects/<project-id>.png` or `.mp4` — swap into the Projects desktop-window previews.
- `certificates/<name>.pdf` — wire into the Certificates file-explorer preview modal.

Nothing here is required for the site to build — every section has a
graceful placeholder until real assets are added.
